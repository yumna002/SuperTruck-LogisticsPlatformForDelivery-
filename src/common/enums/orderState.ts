export enum OrderStateEnum {
  DRAFT = 'draft',
  PENDING = 'pending',
  PROCESSING = 'processing',
  REJECTED = 'rejected',
  CANCELLED = 'cancelled',
  CLOSED = 'closed',
  SCHEDULED = 'scheduled',
}
